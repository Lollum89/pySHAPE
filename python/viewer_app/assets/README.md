This folder is optional.

- Place `app.ico` here to embed a custom icon into the packaged SHAPE-Viewer.exe.
- Any other runtime assets required by the viewer can live here (and will be bundled by the PyInstaller spec).
